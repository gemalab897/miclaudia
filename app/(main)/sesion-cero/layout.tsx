export const metadata = {
  title: "Zero Session | CBT Atlas",
  description: "Complete guide for the first CBT session.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
