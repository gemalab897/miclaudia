export const metadata = {
  title: "Sesión Cero | Mi Claudia",
  description: "Guía completa para la primera sesión TCC.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
